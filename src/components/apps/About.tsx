/**
 * About — placeholder app body. Replace with MDX or rich
 * about content once the design system stabilizes.
 */
export default function About() {
  return (
    <article className="text-14 text-text p-4 font-mono leading-relaxed">
      <h1 className="font-chrome text-24 text-text">hello.</h1>
      <p className="mt-3">
        I build software at the intersection of craft and systems. This site is a working
        retro-desktop environment: every window, taskbar item, and terminal command is real.
      </p>
      <p className="text-text-dim mt-3">{/* edit src/components/apps/About.tsx */}</p>
    </article>
  );
}
